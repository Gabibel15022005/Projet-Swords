using UnityEngine;

public class PlayerAniamtorsManager : MonoBehaviour
{
    // script avec les ref de chaque animator du joueur

    // mettre dans chaque bool dans aniamtor dans des enums qui seront dans EnumsManager exemple : animatorMainBody.SetBool(AnimatorMainBody.isMoving.ToString(), value)

    //créer des Action pour chaque Bool
}
